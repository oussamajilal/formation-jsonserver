{
  "name": "formation-mocks",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "json-server": "^0.16.3",
    "json-server-auth": "^2.1.0"
  },
  "scripts": {
    "start": "json-server-auth --watch db.json  --port 3001"
  }
}
